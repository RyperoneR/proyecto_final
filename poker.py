import random as rand
import PySimpleGUI as sg 

layout = [
    [sg.Button("Abandonar")]
    [sg.Button("Pasar")],
    [sg.Button("Subir Apuesta")],
    [sg.Button("All-in")]
]

ventana = sg.Window("POKER")